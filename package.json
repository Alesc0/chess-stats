{
  "name": "chess-stats",
  "version": "1.1.3",
  "description": "API that returns a chess stats card image for a given username",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun --watch src/index.ts",
    "dev": "bun --hot src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
